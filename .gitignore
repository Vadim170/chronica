# Rust
/core/target/
**/*.rs.bk

# Reference: old project kept as porting source (its own git repo)
/transcriber_webui_project/

# Real recording data / fixtures
/transcripts/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Models (downloaded, large)
/models/
**/Models/

# Apple build
/apple/build/
/apple/.build/
/apple/DerivedData/
*.xcuserstate
xcuserdata/

# Build artifacts (regenerated by apple/Scripts/build-core.sh / package-app.sh)
/apple/dist/
/apple/lib/

# Local-only junk, never project files
/proxies.txt

# macOS
.DS_Store

# Prebuilt Android ML runtimes (~25 MB): downloaded/built by android/build-core.sh,
# never committed — GitHub clones stay small and licences stay with upstream.
/android/app/src/main/jniLibs/

# Agent worktrees
/.claude/
